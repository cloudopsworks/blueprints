##
# (c) 2021-2025
#     Cloud Ops Works LLC - https://cloudops.works/
#     Find us on:
#       GitHub: https://github.com/cloudopsworks
#       WebSite: https://cloudops.works
#     Distributed Under Apache v2.0 License
#
name: Patchwork action AI Agentic Patch Management Process - Java
author: cloudopsworks
description: This action runs the Patchwork AI Agentic Patch Management Process
inputs:
  source_path:
    description: 'The path to the source code'
    required: false
    default: 'source'
  blueprint_path:
    description: 'The path to the blueprint'
    required: false
    default: 'bp'
  token:
    description: 'The Github token'
    required: true

runs:
  using: 'composite'
  steps:
    - name: Generate  patchwork-setting.yaml
      shell: bash
      run: |
        echo "language: java" >> patchwork-setting.yaml 

    # Get pipeline conf java version Defaults to 21
    - name: Get pipeline Java version
      id: java_version
      uses: mikefarah/yq@master
      with:
        cmd: yq e '.java.version // "25"' ./${{ inputs.source_path }}/.cloudopsworks/vars/inputs-global.yaml

    # Get pipeline conf java dist defaults to temurin (adopt is deprecated)
    - name: Get pipeline Java dist
      id: java_dist
      uses: mikefarah/yq@master
      with:
        cmd: yq e '.java.dist // "temurin"' ./${{ inputs.source_path }}/.cloudopsworks/vars/inputs-global.yaml

    # Get pipeline conf java image variant defaults to alpine
    - name: Get pipeline Java image variant
      id: java_image_variant
      uses: mikefarah/yq@master
      with:
        cmd: yq e '.java.image_variant // "alpine"' ./${{ inputs.source_path }}/.cloudopsworks/vars/inputs-global.yaml

    # Get the Package name from pom.xml (in the form of groupId.artifactId)
    - name: Get Package name
      id: package_name
      uses: mikefarah/yq@master
      with:
        cmd: yq e -oy '.project.groupId + "." + .project.artifactId' ${{ inputs.source_path }}/pom.xml

    # Get from global inputs the custom Maven Options
    - name: Get Maven options
      id: maven_opts
      uses: mikefarah/yq@master
      with:
        cmd: yq e -I=0 '.maven_options // ""' ./${{ inputs.source_path }}/.cloudopsworks/vars/inputs-global.yaml

    # Get DependencyTrack project type configuration, default is null
    - name: Get Pipeline conf DependencyTrack Project type
      id: dtrack_project_type
      uses: mikefarah/yq@master
      with:
        cmd: yq e '.dependencyTrack.type // "application"' ./${{ inputs.source_path }}/.cloudopsworks/vars/inputs-global.yaml

    # Setup Java JDK for builds
    - name: Set JDK
      uses: actions/setup-java@v4
      with:
        java-version: "${{ steps.java_version.outputs.result }}"
        distribution: "${{ steps.java_dist.outputs.result }}"

    # Setup Maven for builds
    - name: Set up Maven
      uses: stCarolas/setup-maven@v5
      with:
        maven-version: 3.9.9

    # Cache all maven packages
    - name: Cache Maven packages
      uses: actions/cache@v5
      with:
        path: ~/.m2/repository
        key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
        restore-keys: |
          ${{ runner.os }}-maven-

    # Versioning SET
    - name: Version SET
      working-directory: ${{ inputs.source_path }}
      shell: bash
      run: |
        echo "::group::Maven Version SET"
        make version
        echo "::endgroup::"
      env:
        GITHUB_ACTOR: ${{ inputs.bot_user }}
        GITHUB_TOKEN: ${{ inputs.token }}

    - name: Version Capture
      working-directory: ${{ inputs.source_path }}
      id: semver
      shell: bash
      run: |
        echo "result=$(cat VERSION)" >> $GITHUB_OUTPUT
