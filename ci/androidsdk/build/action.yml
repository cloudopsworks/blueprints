##
# (c) 2021-2025
#     Cloud Ops Works LLC - https://cloudops.works/
#     Find us on:
#       GitHub: https://github.com/cloudopsworks
#       WebSite: https://cloudops.works
#     Distributed Under Apache v2.0 License
#
name: Build Android SDK Sources
author: cloudopsworks
description: Build Android SDK sources and save artifacts
inputs:
  source_path:
    description: 'The path to the source code'
    required: false
    default: 'source'
  blueprint_path:
    description: 'The path to the blueprint'
    required: false
    default: 'bp'
  artifacts_name:
    description: 'The name of the artifacts to save'
    required: false
    default: 'build_artifacts'
  sources_artifacts_name:
    description: 'The name of the sources to save'
    required: false
    default: 'source_artifacts'
  project_key:
    description: 'The release source name, usually the repository name'
    required: true
  bot_user:
    description: 'The bot user to use for the build'
    required: true
  token:
    description: 'The github token to use for the build'
    required: true
  android_version:
    description: 'The Android SDK version to use for the build'
    required: true
  android_sdk:
    description: 'The Android SDK SDK to use for the build'
    required: true
  android_configuration:
    description: 'The Android SDK configuration to use for the build'
    required: true
  android_destination:
    description: 'The Android SDK destination to use for the build'
    required: true
  android_extra_args:
    description: 'The Android SDK extra arguments to use for the build'
    required: false
    default: ''
  android_extra_targets:
    description: 'The Android SDK extra target arguments to use for the build'
    required: false
    default: ''
  android_packages:
    description: 'The Android SDK packages to use for the build'
    required: false
    default: 'tools platform-tools'
outputs:
  semver:
    description: 'The semver version'
    value: ${{ steps.semver.outputs.result }}
  package_name:
    description: 'The package name'
    value: ${{ steps.package_name.outputs.result }}

runs:
  using: 'composite'
  steps:
    - name: Check tools and install
      shell: bash
      run: |
        echo "::group::Check Tools"
        which gh || brew install gh
        which grep || brew install grep
        which yq || brew install yq
        echo "::endgroup::"

    # Versioning SET
    - name: Version SET
      working-directory: ${{ inputs.source_path }}
      shell: bash
      run: |
        echo "::group::Android SDK Version SET"
        make version
        echo "::endgroup::"
      env:
        GITHUB_ACTOR: ${INPUTS_BOT_USER}
        GITHUB_TOKEN: ${INPUTS_TOKEN}

        INPUTS_BOT_USER: ${INPUTS_BOT_USER}
        INPUTS_TOKEN: ${INPUTS_TOKEN}
    - name: Version Capture
      working-directory: ${{ inputs.source_path }}
      id: semver
      shell: bash
      run: |
        echo "result=$(cat VERSION)" >> $GITHUB_OUTPUT

    - name: Setup Base Java Environment
      uses: actions/setup-java@v5
      with:
        distribution: 'temurin'
        java-version: '17'

    - name: Setup Android SDK
      uses: android-actions/setup-android@v3
      with:
        cmdline-tools-version: ${{ inputs.android_version }}
        packages: ${{ inputs.android_packages }}
        accept-android-sdk-licenses: true
        log-accepted-android-sdk-licenses: false

    # Build the code!
    - name: Build Code
      working-directory: ${{ inputs.source_path }}
      if: ${{ contains(fromJSON('["debug","Debug","DEBUG"]'), inputs.android_configuration) }}
      shell: bash
      env:
        GITHUB_ACTOR: ${{ inputs.bot_user }}
        GITHUB_TOKEN: ${{ inputs.token }}
        INPUTS_ANDROID_EXTRA_ARGS: ${{ inputs.android_extra_args }}
        INPUTS_ANDROID_CONFIGURATION: ${{ inputs.android_configuration }}
        INPUTS_ANDROID_EXTRA_TARGETS: ${{ inputs.android_extra_targets }}
      run: |
        echo "::group::Gradle Android SDK Build"
        ./gradlew --no-daemon ${INPUTS_ANDROID_EXTRA_ARGS} clean assemble${INPUTS_ANDROID_CONFIGURATION} ${INPUTS_ANDROID_EXTRA_TARGETS}
        echo "::endgroup::"

    # Test the code!
    - name: Test Code
      working-directory: ${{ inputs.source_path }}
      shell: bash
      env:
        GITHUB_ACTOR: ${{ inputs.bot_user }}
        GITHUB_TOKEN: ${{ inputs.token }}
        INPUTS_ANDROID_CONFIGURATION: ${{ inputs.android_configuration }}
        INPUTS_BOT_USER: ${{ inputs.bot_user }}
        INPUTS_TOKEN: ${{ inputs.token }}
        INPUTS_SOURCE_PATH: ${{ inputs.source_path }}
        INPUTS_ANDROID_SCHEME: ${{ inputs.android_scheme }}
        INPUTS_ARTIFACTS_NAME: ${{ inputs.artifacts_name }}
      run: |
        echo "::group::Gradle Android SDK Run Tests"
        ./gradlew --no-daemon test${INPUTS_ANDROID_CONFIGURATION}
        echo "::endgroup::"

#    - name: Generate IPA - Unsigned
#      shell: bash
#      env:
#        GITHUB_ACTOR: ${INPUTS_BOT_USER}
#        GITHUB_TOKEN: ${INPUTS_TOKEN}
#      run: |
#        echo "::group::XCode Export IPA - Unsigned"
#        tmp_dir=$(mktemp -d)
#        mkdir -p $tmp_dir/Payload/
#        cp -R "${INPUTS_SOURCE_PATH}/build/derived-data/Build/Products/${INPUTS_ANDROID_CONFIGURATION}-iphoneos/${INPUTS_ANDROID_SCHEME}.app" $tmp_dir/Payload/
#        cd $tmp_dir
#        mkdir -p $GITHUB_WORKSPACE/${INPUTS_SOURCE_PATH}/build/${INPUTS_ARTIFACTS_NAME}
#        zip -r $GITHUB_WORKSPACE/${INPUTS_SOURCE_PATH}/build/${INPUTS_ARTIFACTS_NAME}/${INPUTS_ARTIFACTS_NAME}.ipa .
#        echo "::endgroup::"
