##
# (c) 2024 - Cloud Ops Works LLC - https://cloudops.works/
#            On GitHub: https://github.com/cloudopsworks
#            Distributed Under Apache v2.0 License
#
name: Build Java Sources
author: cloudopsworks
description: Build Java sources and save artifacts
inputs:
  source_path:
    description: 'The path to the source code'
    required: false
    default: 'source'
  blueprint_path:
    description: 'The path to the blueprint'
    required: false
    default: 'bp'
  artifacts_name:
    description: 'The name of the artifacts to save'
    required: false
    default: 'build_artifacts'
  sources_artifacts_name:
    description: 'The name of the sources to save'
    required: false
    default: 'source_artifacts'

runs:
  using: 'composite'
  steps:
    # Get pipeline conf java version Defaults to 21
    - name: Get pipeline Java version
      id: java_version
      working-directory: ${{ inputs.source_path }}
      uses: mikefarah/yq@master
      with:
        cmd: yq e '.java.version // "21"' ./.github/vars/inputs-global.yaml

    # Get pipeline conf java dist defaults to temurin (adopt is deprecated)
    - name: Get pipeline Java dist
      id: java_dist
      working-directory: ${{ inputs.source_path }}
      uses: mikefarah/yq@master
      with:
        cmd: yq e '.java.dist // "temurin"' ./.github/vars/inputs-global.yaml

    # Get pipeline conf java image variant defaults to alpine
    - name: Get pipeline Java image variant
      id: java_image_variant
      working-directory: ${{ inputs.source_path }}
      uses: mikefarah/yq@master
      with:
        cmd: yq e '.java.image_variant // "alpine"' ./.github/vars/inputs-global.yaml

    # Get preview enabled
    - name: Get pipeline preview enabled
      id: has_preview
      working-directory: ${{ inputs.source_path }}
      uses: mikefarah/yq@master
      with:
        cmd: yq e '.preview.enabled and true' ./.github/vars/inputs-global.yaml

    # Get if this is a library project
    - name: Get pipeline preview enabled
      id: is_library
      working-directory: ${{ inputs.source_path }}
      uses: mikefarah/yq@master
      with:
        cmd: yq e '.isLibrary and true' ./.github/vars/inputs-global.yaml

    # Get Sonarqube Configuration is enabled
    - name: Get Pipeline conf Sonarqube is Enabled
      id: sonarqube_enabled
      working-directory: ${{ inputs.source_path }}
      uses: mikefarah/yq@master
      with:
        cmd: yq e '.sonarqube.enabled and true' ./.github/vars/inputs-global.yaml

    # Get DependencyTrack Configuration is enabled
    - name: Get Pipeline conf DependencyTrack is Enabled
      id: dtrack_enabled
      working-directory: ${{ inputs.source_path }}
      uses: mikefarah/yq@master
      with:
        cmd: yq e '.dependencyTrack.enabled and true' ./.github/vars/inputs-global.yaml

    # Get DependencyTrack project type configuration, default is null
    - name: Get Pipeline conf DependencyTrack Project type
      id: dtrack_project_type
      working-directory: ${{ inputs.source_path }}
      uses: mikefarah/yq@master
      with:
        cmd: yq e '.dependencyTrack.type // "Application"' ./.github/vars/inputs-global.yaml

    # Get Snyk Configuration is enabled
    - name: Get Pipeline conf Snyk is Enabled
      id: snyk_enabled
      working-directory: ${{ inputs.source_path }}
      uses: mikefarah/yq@master
      with:
        cmd: yq e '.snyk.enabled and true' ./.github/vars/inputs-global.yaml

    # Get Semgrep Configuration is enabled
    - name: Get Pipeline conf Semgrep is Enabled
      id: semgrep_enabled
      working-directory: ${{ inputs.source_path }}
      uses: mikefarah/yq@master
      with:
        cmd: yq e '.semgrep.enabled and true' ./.github/vars/inputs-global.yaml

    # Get the Package name from pom.xml (in the form of groupId.artifactId)
    - name: Get Package name
      id: package_name
      working-directory: ${{ inputs.source_path }}
      uses: mikefarah/yq@master
      with:
        cmd: yq e -oy '.project.groupId + "." + .project.artifactId' pom.xml

    # Get from global inputs the custom Maven Options
    - name: Get Maven options
      id: maven_opts
      working-directory: ${{ inputs.source_path }}
      uses: mikefarah/yq@master
      with:
        cmd: yq e -I=0 '.maven_options // ""' ./.github/vars/inputs-global.yaml

    # Get the owner repo
    - name: Get owner
      id: getowner
      run: |
        repo_owner=`echo "$GITHUB_REPOSITORY" | cut -f 1 -d "/"`
        repo_name=`echo "$GITHUB_REPOSITORY" | cut -f 2 -d "/"`
        echo "owner=$repo_owner" >> $GITHUB_OUTPUT
        echo "repo_name=$repo_name" >> $GITHUB_OUTPUT

    # Cache all maven packages
    - name: Cache Maven packages
      uses: actions/cache@v4
      with:
        path: ~/.m2/repository
        key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
        restore-keys: |
          ${{ runner.os }}-maven-

    # Versioning SET
    - name: Version SET
      working-directory: ${{ inputs.source_path }}
      run: make version
      env:
        GITHUB_ACTOR: ${{ inputs.BOT_USER }}
        GITHUB_TOKEN: ${{ secrets.BOT_TOKEN }}

    - name: Version Capture
      working-directory: ${{ inputs.source_path }}
      id: semver
      run: |
        echo "version=$(cat VERSION)" >> $GITHUB_OUTPUT

    # Build the code!
    - name: Build Code
      working-directory: ${{ inputs.source_path }}
      run: |
        MAVEN_OPTS="$MAVEN_OPTS ${{ steps.maven_opts.outputs.result }}"
        mvn --batch-mode --update-snapshots clean package
      env:
        GITHUB_ACTOR: ${{ inputs.BOT_USER }}
        GITHUB_TOKEN: ${{ secrets.BOT_TOKEN }}

    # Deploy release to Maven Repository (defaults to Github repo)
    - name: Deploy to Maven Repo
      if: ${{ steps.is_library.outputs.result == 'true' }}
      working-directory: ${{ inputs.source_path }}
      run: |
        MAVEN_OPTS="$MAVEN_OPTS ${{ steps.maven_opts.outputs.result }}"
        mvn --batch-mode deploy
      env:
        GITHUB_ACTOR: ${{ inputs.BOT_USER }}
        GITHUB_TOKEN: ${{ secrets.BOT_TOKEN }}
